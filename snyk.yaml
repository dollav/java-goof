version: 2
customFilters:
  filter: ".vulnerabilities |= map(if .type == \"license\" and .title == \"LGPL-2.0 license\" then empty else . end)"
  pass: "[.vulnerabilities[] | select(.type != \"license\")] | length"
  msg: "Vulnerabilities found"
